This program uses textures to display a cute image on each face of a rotating cube. To view the page, download node.js.
Enter the directory of this project on your terminal and use the command "npm install http-server -g", followed by
"npm http-server -o". This should open a new window in your preferred web browser to view the page.